Html tags:
    meta, link, script, ul, li, a, div, h, p and span.

CSS properties:
    display, flex-direction, position, right and top.

Use of JS and Bootstrap:
    I used Javascript along with Bootstrap to implement a button that toggles between dark/light mode on the pages.
